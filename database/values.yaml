replicaCount: 1

image:
  repository: postgres
  tag: "15"

postgres:
  db: class_schedule
  user: admin
  password: admin

persistence:
  enabled: true
  accessMode: ReadWriteOnce
  size: 1Gi

service:
  type: ClusterIP
  port: 5432